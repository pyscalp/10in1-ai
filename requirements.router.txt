# Core router dependencies for Nebius Serverless AI deployment.
# Excludes heavy optional tools (browser-use, playwright, maxun) and the
# Supabase REST client (we use psycopg2 + pgvector directly) to keep the
# container image small and cold-starts fast.

--extra-index-url https://download.pytorch.org/whl/cpu

# Web framework
fastapi>=0.115.0,<0.120
uvicorn[standard]>=0.32.0,<0.35
python-dotenv>=1.0.1
pydantic>=2.10.0,<3.0
pydantic-settings>=2.6.0
httpx>=0.27.2,<0.29

# Embeddings + vector store
sentence-transformers>=3.0.0
psycopg2-binary>=2.9.10
numpy>=1.26.0

# Misc utils
pypdf>=5.2.0
pdfplumber>=0.11.4
python-multipart>=0.0.17
